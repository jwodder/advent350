- Test everything!
- Get rid of as many global variables as possible
- Try to completely rewrite the system for keeping track of object locations
- Remove fields that are always the same when saved (gaveup, foobar, ???) from
  `Game` (or just from its pickling method?)
- Ensure the code works in both Python 2 and Python 3
- Get the code to pass pylint
- If the user enters "save" without a filename while playing a restored game,
  save the game to the same path that it was restored from?
- Add a command-line option for specifying the default save file path
- Add docstrings
- Add error handling for when restoring a saved game fails
- Use constants in place of as many magic numbers as possible/reasonable
 - Convert the first column of the vocab table into a class with an enum for
   wordtype
- Add an option for using the original RNG with a specified seed/datime for the
  purposes of reproducibility/testing?
